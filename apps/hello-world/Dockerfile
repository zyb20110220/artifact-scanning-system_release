# ============================================================
# Dockerfile —— hello-world 验证应用（阶段 0.6）
# 基于 Nginx，仅用于验证：构建 → Harbor 推送 → Helm 部署
# ============================================================
FROM nginx:alpine

COPY index.html /usr/share/nginx/html/index.html

EXPOSE 80
