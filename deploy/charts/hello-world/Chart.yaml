# ============================================================
# Chart.yaml —— hello-world 验证应用 Chart（阶段 0.6）
# ============================================================
apiVersion: v2
name: hello-world
description: 阶段 0.6 验证应用 —— 首个 Helm Chart 部署验证
type: application
version: 0.1.0
appVersion: "1.0.0"
